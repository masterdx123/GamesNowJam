namespace Enums
{
    // TODO: Define stats (Should we differentiate between weapon and character?)
    public enum Stat
    {
        FireRate,
        Damage,
        CriticalHitChance,
        ProjectileSpread
    }
}
